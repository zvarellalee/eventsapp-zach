Modify the api-deployment.yaml
Add the lines below to the end of the file:

       env:
       - name: DBHOST
         value: "database-server-mariadb.default"
       - name: DBUSER
         value: "root"
       - name: DBPASSWORD
         valueFrom:
           secretKeyRef:
             name: database-server-mariadb
             key: mariadb-root-password
       - name: DBDATABASE
         value: "events_db" 

